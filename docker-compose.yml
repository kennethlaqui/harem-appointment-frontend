version: '3'

services:
  site:
   build: .
   stdin_open: true
   tty: true
   ports:
     - "8080:8080"
   volumes:
     - "/app/node_modules" 
     - ./:/app
   #environment:
   #  - NODE_ENV=development
   #  - CHOKIDAR_USEPOLLING=true
